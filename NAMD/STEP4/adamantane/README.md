Using adamantane as an example, the relevant surface-hopping simulation codes are provided in the corresponding folder.

The entropy analysis based on different surface-hopping populations is included in the `step4_entropy.ipynb` notebook, where the mSDM simulation data are used as an example.